# Scanner evidence

Raw scanner output, commands, timestamps, durations, versions, and exit statuses belong under the corresponding scanner directory. This initial repository contains no scanner output because the scanners were unavailable during setup.
