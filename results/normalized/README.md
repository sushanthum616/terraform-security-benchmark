# Normalized results

`observed-findings.json` is intentionally an empty evidence manifest until a scanner actually runs. Run `scripts/normalize_results.py` after adding only observed findings with the required fields. Do not populate expected findings as if they were scanner observations.
