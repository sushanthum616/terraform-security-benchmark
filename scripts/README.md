# Scripts

- `normalize_results.py` converts an observed JSON evidence manifest into the common CSV schema. It does not invent findings or run a scanner.

Scanner command transcripts should be saved alongside raw outputs once the tools are installed.
